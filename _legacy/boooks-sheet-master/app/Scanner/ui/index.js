// import and export all camera ui here so camera only needs one ui import!

// We need drawers in here also so we can use that for input for manual data here as well.

//#1 thing is when open file it should open drawer and attach file..? Or should that be when book is added? Hmm?

//Should have and file input in scanner ? Or only be when editing Book?